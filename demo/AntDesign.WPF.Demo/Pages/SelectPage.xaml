<UserControl x:Class="AntDesign.WPF.Demo.Pages.SelectPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="800"
             d:DesignHeight="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24" MaxWidth="800">

            <!-- Page Title -->
            <TextBlock Text="{DynamicResource Select.Title}"
                       FontSize="{DynamicResource AntDesign.FontSize.H3}"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource AntDesign.Brush.Text}"
                       Margin="0,0,0,8" />
            <TextBlock Text="{DynamicResource Select.Desc}"
                       FontSize="{DynamicResource AntDesign.FontSize}"
                       Foreground="{DynamicResource AntDesign.Brush.Text.Secondary}"
                       Margin="0,0,0,32" />

            <!-- Section: Basic Select -->
            <TextBlock Text="{DynamicResource Select.BasicUsage}"
                       FontSize="{DynamicResource AntDesign.FontSize.LG}"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource AntDesign.Brush.Text}"
                       Margin="0,0,0,16" />

            <Border Background="{DynamicResource AntDesign.Brush.Bg.Container}"
                    BorderBrush="{DynamicResource AntDesign.Brush.Border.Secondary}"
                    BorderThickness="1"
                    CornerRadius="{DynamicResource AntDesign.CornerRadius}"
                    Padding="24"
                    Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Select.BasicComboBox}"
                               Foreground="{DynamicResource AntDesign.Brush.Text.Secondary}"
                               Margin="0,0,0,8" />
                    <ComboBox Width="240"
                              HorizontalAlignment="Left"
                              SelectedIndex="0"
                              Margin="0,0,0,16">
                        <ComboBoxItem Content="{DynamicResource Select.Option1}" />
                        <ComboBoxItem Content="{DynamicResource Select.Option2}" />
                        <ComboBoxItem Content="{DynamicResource Select.Option3}" />
                        <ComboBoxItem Content="{DynamicResource Select.Option4}" />
                    </ComboBox>

                    <TextBlock Text="{DynamicResource Select.Editable}"
                               Foreground="{DynamicResource AntDesign.Brush.Text.Secondary}"
                               Margin="0,0,0,8" />
                    <ComboBox Width="240"
                              HorizontalAlignment="Left"
                              IsEditable="True"
                              Text="{DynamicResource Select.TypeOrSelect}"
                              Margin="0,0,0,0">
                        <ComboBoxItem Content="Apple" />
                        <ComboBoxItem Content="Banana" />
                        <ComboBoxItem Content="Cherry" />
                        <ComboBoxItem Content="Durian" />
                    </ComboBox>
                </StackPanel>
            </Border>

            <!-- Section: Disabled Select -->
            <TextBlock Text="{DynamicResource Select.DisabledState}"
                       FontSize="{DynamicResource AntDesign.FontSize.LG}"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource AntDesign.Brush.Text}"
                       Margin="0,0,0,16" />

            <Border Background="{DynamicResource AntDesign.Brush.Bg.Container}"
                    BorderBrush="{DynamicResource AntDesign.Brush.Border.Secondary}"
                    BorderThickness="1"
                    CornerRadius="{DynamicResource AntDesign.CornerRadius}"
                    Padding="24"
                    Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Select.DisabledComboBox}"
                               Foreground="{DynamicResource AntDesign.Brush.Text.Secondary}"
                               Margin="0,0,0,8" />
                    <ComboBox Width="240"
                              HorizontalAlignment="Left"
                              IsEnabled="False"
                              SelectedIndex="0"
                              Margin="0,0,0,0">
                        <ComboBoxItem Content="{DynamicResource Select.Option1Disabled}" />
                        <ComboBoxItem Content="{DynamicResource Select.Option2}" />
                    </ComboBox>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
