<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Ant Design WPF — TreeView / TreeViewItem Styles
        Design:
          - TreeView     : no border, container bg
          - TreeViewItem : padding 4,0; left-indent via Margin on ItemsHost
          - Hover        : Fill.Secondary bg
          - Selected     : Primary.Bg bg + Primary fg
          - Toggle       : small arrow glyph (▸ collapsed / ▾ expanded), rotated via RenderTransform
    -->

    <!-- =====================================================================
         TreeView — implicit style
         ===================================================================== -->
    <Style TargetType="{x:Type TreeView}">
        <Setter Property="Background"          Value="{DynamicResource AntDesign.Brush.Bg.Container}" />
        <Setter Property="BorderThickness"     Value="0" />
        <Setter Property="BorderBrush"         Value="{x:Null}" />
        <Setter Property="Padding"             Value="0" />
        <Setter Property="FontSize"            Value="{DynamicResource AntDesign.FontSize}" />
        <Setter Property="Foreground"          Value="{DynamicResource AntDesign.Brush.Text}" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility"   Value="Auto" />
        <Setter Property="FocusVisualStyle"    Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeView}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer Focusable="False"
                                      Padding="{TemplateBinding Padding}"
                                      HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                      VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- =====================================================================
         TreeViewItem — implicit style
         Template anatomy:
           Grid (2 cols: Auto toggle + * content)
             Row 0: [toggle button] [header row Border]
             Row 1: (colspan 2)     [children ItemsPresenter]
         ===================================================================== -->
    <Style TargetType="{x:Type TreeViewItem}">
        <Setter Property="Padding"             Value="4,0" />
        <Setter Property="Foreground"          Value="{DynamicResource AntDesign.Brush.Text}" />
        <Setter Property="Background"          Value="Transparent" />
        <Setter Property="FontSize"            Value="{DynamicResource AntDesign.FontSize}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment"   Value="Center" />
        <Setter Property="IsTabStop"           Value="False" />
        <Setter Property="FocusVisualStyle"    Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                    <StackPanel>
                        <!-- Header row -->
                        <Border x:Name="HeaderBorder"
                                Background="Transparent"
                                BorderThickness="0"
                                CornerRadius="{DynamicResource AntDesign.CornerRadius.SM}"
                                Margin="0,1"
                                Padding="0"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <!-- Toggle arrow -->
                                    <ColumnDefinition Width="20" />
                                    <!-- Item content -->
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Expand / collapse toggle -->
                                <ToggleButton x:Name="ExpandToggle"
                                              Grid.Column="0"
                                              Width="20"
                                              Height="20"
                                              Focusable="False"
                                              IsChecked="{Binding IsExpanded,
                                                          RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press"
                                              FocusVisualStyle="{x:Null}"
                                              Cursor="Hand">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <!-- Arrow glyph: ▸ right-pointing, rotated 90° when expanded -->
                                            <Border Background="Transparent"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Width="20" Height="20">
                                                <Path x:Name="ArrowPath"
                                                      Data="M 3,5 L 10,12 L 17,5"
                                                      Stroke="{DynamicResource AntDesign.Brush.Text.Secondary}"
                                                      StrokeThickness="1.5"
                                                      StrokeLineJoin="Round"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      RenderTransformOrigin="0.5,0.5"
                                                      Stretch="Uniform"
                                                      Width="10" Height="10">
                                                    <Path.RenderTransform>
                                                        <!-- Collapsed: –90° (arrow points right ▸) -->
                                                        <RotateTransform x:Name="ArrowRotate" Angle="-90" />
                                                    </Path.RenderTransform>
                                                </Path>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="ArrowPath" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <RotateTransform Angle="0" />
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>

                                <!-- Item header content -->
                                <Border Grid.Column="1"
                                        Padding="{TemplateBinding Padding}"
                                        Background="Transparent">
                                    <ContentPresenter x:Name="HeaderContent"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      TextElement.Foreground="{DynamicResource AntDesign.Brush.Text}" />
                                </Border>
                            </Grid>
                        </Border>

                        <!-- Children (only visible when expanded) -->
                        <ItemsPresenter x:Name="ItemsHost"
                                        Margin="20,0,0,0"
                                        Visibility="Collapsed" />
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background"
                                    Value="{DynamicResource AntDesign.Brush.Fill.Secondary}" />
                        </Trigger>
                        <!-- Selected (not focused) -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="HeaderBorder" Property="Background"
                                    Value="{DynamicResource AntDesign.Brush.Primary.Bg}" />
                            <Setter TargetName="HeaderContent" Property="TextElement.Foreground"
                                    Value="{DynamicResource AntDesign.Brush.Primary}" />
                        </Trigger>
                        <!-- Selected + hover -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected"  Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="HeaderBorder" Property="Background"
                                    Value="{DynamicResource AntDesign.Brush.Primary.Bg}" />
                            <Setter TargetName="HeaderContent" Property="TextElement.Foreground"
                                    Value="{DynamicResource AntDesign.Brush.Primary}" />
                        </MultiTrigger>
                        <!-- Expanded: show children -->
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <!-- No children: hide toggle -->
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="ExpandToggle" Property="Visibility" Value="Hidden" />
                        </Trigger>
                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="HeaderContent" Property="TextElement.Foreground"
                                    Value="{DynamicResource AntDesign.Brush.Text.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
